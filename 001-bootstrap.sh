# Automatically generated (do not change)
C3_ENV_VERSION="3.30.0"

# Allows committing to worktrees without a .pre-commit-config.yaml file
# when the [pre-commit framework](https://pre-commit.com/) is already installed.
export PRE_COMMIT_ALLOW_NO_CONFIG=1
